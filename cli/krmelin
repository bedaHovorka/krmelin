#!/usr/bin/env sh
# Launcher script for Krmelin.
# After `./gradlew shadowJar`, run: cli/krmelin [args]
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
JAR="$(find "$SCRIPT_DIR/../build/libs" -maxdepth 1 -name 'krmelin-*.jar' ! -name '*-slim.jar')"
exec java -jar "$JAR" "$@"
